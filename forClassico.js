for(let i=1;i<=10;i++){
    let tipo = i % 2 === 0 ? 'par' : 'impar'
    console.log(`${i} - ${tipo}`) 
}

const frutas = ['maçã', 'uva', 'pera', 'laranja' ]
for (let i=0 ; i<frutas.length; i++){
    console.log(frutas[i])
}